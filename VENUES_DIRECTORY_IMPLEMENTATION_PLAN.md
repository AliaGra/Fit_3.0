# План поетапного впровадження: довідник закладів (клуби, студії, секції) — FIT 3.0

**Призначення:** єдиний дорожній документ для розробки, перевірки та обліку **комітів і пушів**. Після кожного значущого кроку оновлюйте статуси, SHA коміта та дату.

**Пов’язані джерела:**
- Таксономія кодів: `fit_club_directory.md` (`organization_type`, `studio_type`, `section_type`, `group_class_type`).
- Застаріле/частково суперечливе ТЗ (обладнання клубів, інша схема): `TZ_FitnessClubs_FIT3.md` — при впровадженні **узгодити або явно замінити** рішеннями з цього плану.

---

## Легенда статусів

| Статус | Значення |
|--------|----------|
| ⬜ | Не почато |
| 🔄 | У роботі |
| ✅ | Зроблено, перевірено локально |
| 🚀 | Закомічено **і** запушено в віддалений репозиторій |

**Як відмічати:** замініть ⬜ на 🔄 під час роботи, після merge в `main` — ✅ або 🚀 і додайте рядок у [Журнал комітів](#журнал-комітів).

---

## Зафіксовані вимоги (коротко)

1. **БД:** для кожного закладу за можливості є **address** (текст) і **lat/lng** з Telegram; **без координат запис заборонено**. Якщо адреси немає — у UI показувати карту/відстань без рядка адреси.
2. **Координати** вводить лише **оператор** через бота **@FitHad_adminbot** (немає веб-адмінки; імпорт координат «з адмінки» не передбачено).
3. **Транслит:** runtime-транслит **лише для пошуку по UA-назві** (нормалізація запиту).
4. **Два режими пошуку** (не взаємовиключні як логіка продукту — користувач обирає сценарій):
   - **Гео:** коло + радіус + **усі релевантні фільтри** з довідника (тип організації, за потреби studio/section, групові заняття тощо).
   - **Текст + область/місто + фільтри:** назва (з транслитом) + обмеження по регіону + **ті самі фільтри**, що й у гео-режимі.
5. **Пошук доступний усім ролям:** учень, тренер, користувач без тренера.
6. **Розклад:** на першому етапі — лише таблиці в БД + ручне наповнення (SQL); UI бота — пізніше.

---

## Фаза 0 — Підготовка та узгодження

| # | Крок | Статус | Коміт | Примітки |
|---|------|--------|-------|----------|
| 0.1 | Підтвердити ім’я таблиць і префікси callback/FSM у `constants.js` (чернетка в кінці цього файлу або в `CALLBACK_FSM_MODULE_MATRIX.md`) | 🚀 | `1be57cc` | `VENUES_*`, `REG_VENUE_*`, `PROFILE_COACH_VENUES`, FSM у `constants.js` |
| 0.2 | Рішення щодо перетину з `TZ_FitnessClubs_FIT3.md` (обладнання клубу / `user_clubs` / плани): об’єднати, відкласти або замінити | ⬜ | | Окреме рішення в документації |
| 0.3 | Перевірити наявність `city_list` / полів `oblast`, `city` у `users` для узгодження фільтрів | 🚀 | `1be57cc` | У `users`: `city`, `oblast`, `district` (міграція `supabase_migration_users_venues_location_notify.sql`); у `venues`: `district`; реєстрація/профіль збирають район після вибору НП; сповіщення про новий заклад за oblast+city+(district) |

---

## Фаза 1 — Схема БД (Supabase migrations)

| # | Крок | Статус | Коміт | Примітки |
|---|------|--------|-------|----------|
| 1.1 | Таблиця **закладів** (venue): `id`, `name_ua` (NOT NULL), `oblast`, `city` (узгодити з `city_list` — FK або текст + індекси), `address` (nullable), `latitude` NOT NULL, `longitude` NOT NULL, `telegram_url` / `instagram_url` (nullable), `is_active`, `created_at`, `updated_at`, опційно `created_by_operator_chat_id` | 🚀 | `1be57cc` | Файл: `supabase_migration_venues.sql` — застосувати в Supabase |
| 1.2 | Зв’язок **заклад ↔ типи/теги** (many-to-many або масиви — за обраною нормалізацією): мінімум підтримка фільтрів з `fit_club_directory.md` (organization, studio_type, section_type, group_class) | 🚀 | `1be57cc` | `venue_facets` + `organization_type` на `venues` |
| 1.3 | Індекси для пошуку: `(oblast, city)`, gist/geography для geo-запитів **або** формули haversine + btree по lat/lng (обрати один підхід і задокументувати) | 🚀 | `1be57cc` | У JS: `lat/lng` + haversine; у SQL — індекси |
| 1.4 | Таблиці **розкладу** (MVP без UI): сутності під імпорт SQL (наприклад `venue_schedule` або нормалізовані `schedule_slots` — зафіксувати в міграції) | 🚀 | `1be57cc` | `venue_schedule` у міграції |
| 1.5 | Прив’язка **тренер → заклад(и)** junction (наприклад `coach_venues`: `coach_chat_id`, `venue_id`, `is_primary?`, timestamps) | 🚀 | `1be57cc` | `coach_venues` |
| 1.6 | Прив’язка **користувач → обраний заклад** (реєстрація / профіль), якщо відрізняється від тренерської — за потреби `user_venues` або поле в `users` | 🚀 | `1be57cc` | `user_venues` |

**Файли:** `supabase_migration_venues_*.sql` (імена узгодити в команді).

---

## Фаза 2 — Довідники (seed)

| # | Крок | Статус | Коміт | Примітки |
|---|------|--------|-------|----------|
| 2.1 | SQL-seed або окремий скрипт: коди з `fit_club_directory.md` для довідників (organization, studio, section, group_class) з назвами UA | 🚀 | `1be57cc` | `supabase_migration_venues_seed_directory.sql` |
| 2.2 | Перевірка цілісності FK між закладами та довідниками | 🚀 | `1be57cc`+ | FK у БД не обов’язкові; валідація при збереженні фасетів / виборі кодів у `lib/supabase.js` + адмін/основний бот |

---

## Фаза 3 — Шар даних (Node / `lib/supabase.js`)

| # | Крок | Статус | Коміт | Примітки |
|---|------|--------|-------|----------|
| 3.1 | CRUD закладів (для операторського бота): створення лише з lat/lng; оновлення address/соцмереж/тегів | 🚀 | `1be57cc` | `insertVenue`, `updateVenue`, `replaceVenueFacets` |
| 3.2 | Пошук **текст + область/місто + фільтри** (+ runtime транслит UA-назви для матчингу) | 🚀 | `1be57cc` | `searchVenues` |
| 3.3 | Пошук **geo: точка користувача + радіус + ті самі фільтри** | 🚀 | `1be57cc` | `searchVenues` + `radiusKm` |
| 3.4 | Методи прив’язки тренера до закладів; читання для картки профілю | 🚀 | `1be57cc` | `linkCoachVenue`, `getCoachVenues`, … |
| 3.5 | Допоміжна функція транслиту (UA → латиниця) для пошуку — окремий модуль, тести за бажанням | 🚀 | `1be57cc` | `lib/translitUa.js` |

---

## Фаза 4 — Бот оператора (@FitHad_adminbot)

| # | Крок | Статус | Коміт | Примітки |
|---|------|--------|-------|----------|
| 4.1 | Авторизація: лише дозволені `chat_id` / роль оператора (як у help-bot або окремий список) | 🚀 | `1be57cc` | Як у `adminBot`: `ADMIN_CHAT_ID` |
| 4.2 | Сценарій: створення закладу — кроки **назва UA → область/місто → (опційно адреса) → обов’язково location message** → теги/типи → соцмережі | 🚀 | `19555d3` | Групові заняття: лише вибір з довідника (кнопки + пагінація), див. `1be57cc` + `19555d3` |
| 4.3 | Редагування існуючого закладу (адреса, теги, соцмережі; зміна координат — повторна геоточка) | 🚀 | див. WORKLOG 04–13.04.2026 | Картка зі **«Списку закладів»** (`ADM_VVW:*`): контакти, години, розклад групових, amenities, видалення; **превʼю** перед першим збереженням + **✏️** по полях (`ADM_VE:*`); вибір області/міста через `city_list` (`ADM_VOB`/`ADM_VCT`); локальні назви групових (`ADM_VGCUST`); міграції: phone, amenities, `venue_hours` |
| 4.4 | Роутинг: окремий entrypoint у коді (наприклад `lib/adminVenuesBot.js` + webhook), якщо репозиторій спільний з основним ботом | 🚀 | `1be57cc` | Той самий `/admin_webhook`, `adminBot` + `adminVenues` |
| 4.5 | **Довідник цін закладу** (інформативно в адмін-боті; без оплат у основному боті) | 🚀 | `808683c` | Таблиці `venue_group_class_prices`, `venue_gym_membership_offers`, `venue_ancillary_services`; callbacks `ADM_VPR*`; фікс `venue_id` у callback після рестарту — `667e618` |

---

## Фаза 5 — Основний бот: пошук закладів (усі ролі)

| # | Крок | Статус | Коміт | Примітки |
|---|------|--------|-------|----------|
| 5.1 | Підменю **«Клуби, студії»** з головного меню | 🚀 | `1be57cc` | `menu.js` |
| 5.2 | Майстер пошуку: вибір режиму **Гео** / **За назвою та містом**; кроки фільтрів **однакові за складом** | 🚀 | `1be57cc` | `lib/venues.js` (фільтр типу організації; studio/section/group — у шарі, повний UI пізніше) |
| 5.3 | Гео-режим: запит `sendLocation` або використання останньої локації (узгодити UX); радіус за замовчуванням + зміна | 🚀 | `1be57cc` | `1/3/5/10 км` |
| 5.4 | Відображення результату: карта/відстань; рядок адреси лише якщо `address` заповнено | 🚀 | `1be57cc` | |
| 5.5 | Пагінація / обмеження довжини повідомлень Telegram | 🚀 | `1be57cc` | Обмеження показу (наприклад 8 + навігація) |
| 5.6 | Хаб **«Клуби, студії»**: блок «Мої заклади», **🔎 Пошук нового закладу** (`VENUES_SEARCH_NEW`), deep link **`venue_<id>`** (тренер/учень з прив’язкою) | 🚀 | `e181158` | `getUserVenues` + `isPrimary`; спрощення UX (без дубля кнопки «Картка» в рядку) — `1095ac0` |
| 5.7 | Текстовий пошук: область/місто з профілю / реєстрації без повторного вводу | 🚀 | див. WORKLOG 07.04.2026 | `lib/venues.js` (`VENUES_TEXT`) |
| 5.8 | Картка закладу в основному боті: **Тренери закладу** + інлайн **`PVCH`**, публічна картка тренера; deep link **`pvch_<chatId>`**; для тренера прибрано кнопку «на себе» | 🚀 | `07c8aa8`–`05ba0a3` | `lib/venues.js`, `lib/coach.js`, `lib/profile.js` |

---

## Фаза 6 — Прив’язка тренера «де тренує»

| # | Крок | Статус | Коміт | Примітки |
|---|------|--------|-------|----------|
| 6.1 | UI в профілі тренера: вибір одного або кількох закладів з каталогу (пошук як у фазі 5) | 🚀 | `1be57cc` | `PROFILE_COACH_VENUES` + пошук; `linkCoachVenue` |
| 6.2 | Відображення для учня (картка тренера / заклади) — за наявністю продуктової вимоги | 🚀 | `07c8aa8`+ | Тренери на картці закладу (`PVCH`); блоки тренерів у профілі учня за закріпленими закладами; публічна картка тренера |

---

## Фаза 7 — Реєстрація користувача

| # | Крок | Статус | Коміт | Примітки |
|---|------|--------|-------|----------|
| 7.1 | Після введення області/міста — підказка про заклади в цьому місті + список/пошук + **«обрати заклад»** / **«повернутися до пошуку пізніше»** | 🚀 | `1be57cc` | `showRegistrationVenueOffer` |
| 7.2 | Збереження вибору в профілі (зв’язок user ↔ venue) без блокування реєстрації | 🚀 | `1be57cc` | `setUserPrimaryVenue`, `VENUES_PICK` |

---

## Фаза 8 — Розклад закладу (`venue_schedule`, `venue_hours`)

| # | Крок | Статус | Коміт | Примітки |
|---|------|--------|-------|----------|
| 8.1 | Документувати формат ручного SQL-імпорту для розкладу (bulk / інтеграції) | ⬜ | | Таблиці `venue_schedule`, `venue_hours` у міграціях; **адмін-UI вже є** — масовий імпорт окремо за потреби |
| 8.2 | UI для графіку та розкладу групових у **адмін-картці** закладу | 🚀 | див. WORKLOG 08.04.2026 | Графік роботи по днях тижня; розклад групових по кожному `group_class`; не плутати з розкладом **тренера** в профілі (групові — окремий модуль у `lib/profile.js`) |

---

## Документація проєкту після релізу

Після стабілізації фаз перенести в:
- `CHANGELOG.md`, `Зміни_логіки_та_функціоналу.md`, `Бізнес-логіка_Gym_3_0_v1.1.md`, `CALLBACK_FSM_MODULE_MATRIX.md`
- за потреби оновити `WORKLOG.md` (чернетка → `ready` після коміта).

---

## Журнал комітів

Додавайте рядки знизу (найновіші внизу або вгорі — оберіть один стиль і тримайте його).

| Дата | Гілка | Коротко що зроблено | Commit SHA | Пуш |
|------|-------|---------------------|------------|-----|
| 2026-04-02 | main | Venues: міграції + seed, `supabase`/`translitUa`/`venues`/`adminVenues`, меню, реєстрація, профіль тренера; також `arm_flex` міграція у коміті | `1be57cc` | так |
| 2026-04-02 | main | Док: журнал і статуси плану venues | `f061076` | так |
| 2026-04-02 | main | Адмін-бот: групові заняття — лише вибір з довідника (`ADM_VGT`/`ADM_VGP`/`ADM_VGOK`/`ADM_VGCL`), `editMessageText` | `19555d3` | так |
| 2026-04-02 | main | Док: handoff `HANDOFF_VENUES_AND_SEARCH.md`, WORKLOG, CHANGELOG, матриця callback v1.7, план venues | `8d702c3` | так |
| 2026-04-07 … 2026-04-13 | main | Розширення venues: текстовий пошук з профілю, адмін `city_list`, amenities, превʼю/редагування, картка закладу (години, групові, телефон), видалення закладу, `PVCH`/deep links, хаб «Клуби» + `VENUES_SEARCH_NEW`, довідник цін у адмінці (`ADM_VPR*`) | див. `WORKLOG.md` | так |

---

## Ідентифікатори (реалізовано в коді)

- **Основний бот:** `VENUES_*`, `REG_VENUE_*`, `PROFILE_COACH_VENUES`, `VENUES_CARD`, `VENUES_SEARCH_NEW` (`VENUES_SN`), `PVCH`, deep link `venue_*` / `pvch_*` — `lib/constants.js`, `lib/venues.js`, `lib/router.js`.
- **Адмін-бот (заклади):** префікс `ADM_V*` — `ADM_VMENU`, `ADM_VADD`, `ADM_VORG`, `ADM_VGT`, `ADM_VGP`, `ADM_VGOK`, `ADM_VGCL`, `ADM_VVW`, `ADM_VHRS`, `ADM_VSCH`, `ADM_VPR` (ціни), `ADM_VDEL`, `ADM_VE:*` (редагування полів), `ADM_VOB`/`ADM_VCT` (область/місто), `ADM_VAT` (amenities), … — `lib/adminVenues.js`.
- FSM: `venue_search_hub`, `venue_wait_location`, `venue_text_search`, `reg_venue_offer`.

---

**Версія документа:** 1.3  
**Останнє оновлення:** 2026-04-13
