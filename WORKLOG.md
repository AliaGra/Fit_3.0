# WORKLOG — чернетка змін (FIT 3.0)

Цей файл — **робоча пам’ять** про зміни під час розробки.

Правило:
- **Сюди** можна писати все (в т.ч. незавершене).
- **В документацію проєкту** (`CHANGELOG.md`, `README.md`, `Зміни_логіки_та_функціоналу.md`, `docs/*`) переносимо **лише те, що вже закомічено** і має статус `ready`/`shipped`.

Статуси:
- `draft` — в роботі / ще може змінитись / не готово
- `ready` — завершено, закомічено, готово до релізу/мерджу
- `shipped` — вже в `main`/на проді (за потреби)

---

## Шаблон запису

### YYYY-MM-DD HH:MM — <коротка назва зміни>  _(status: draft|ready|shipped)_

- **Scope**: тренер / учень / адмін / БД / деплой
- **Екран / меню**: (напр. `Тренер → Розклад → Мій розклад → Вільні слоти`)
- **Що змінилось**:
  - …
  - …
- **Callback/FSM** (якщо є): (напр. `SCH_...`, `REG_...`)
- **Файли**: (напр. `lib/schedule.js`, `lib/constants.js`)
- **Тест-план (мінімум)**:
  - …
- **Commit**: `<sha або "pending">`
- **Нотатки / ризики**:
  - …

---

## Поточна робота (не закомічено)

> Тримай тут 1–5 активних пунктів, щоб нічого не загубити.

### 2026-08-28 18:58 — Реєстрація тренера: сайт → Telegram → кабінет  _(status: draft)_

- **Scope**: тренер / сайт / БД / деплой
- **Екран / меню**: `mayag.fit/cabinet.html` → Telegram → реєстрація тренера
- **Що змінилось**:
  - Додано форму номера телефону на сайті та одноразову заявку з терміном дії 15 хвилин.
  - Додано підтвердження власного Telegram-контакту з перевіркою номера.
  - Після підтвердження запускається окремий потік реєстрації тренера, без обходу існуючої логіки анкети.
  - Після завершення анкети сайт автоматично отримує й показує витринку тренера.
- **Callback/FSM**: `REG_WEB_CONTACT`, deep-link `/start web_<token>`
- **Файли**: `supabase_migration_web_cabinet_onboarding.sql`, `lib/registration.js`, `lib/router.js`, `lib/helpers.js`, `lib/supabase.js`, `lib/user.js`
- **Тест-план (мінімум)**:
  - Виконати SQL-міграцію в Supabase.
  - Перевірити сайт → номер → Telegram-контакт → повну реєстрацію тренера.
  - Перевірити помилковий номер, повторне використання та протермінований токен.
  - Перевірити автоматичне відображення витринки після повернення на сайт.
- **Commit**: `pending`
- **Нотатки / ризики**:
  - Для бота й Cloudflare потрібен `SUPABASE_SERVICE_ROLE_KEY`.
  - Не переносити в `CHANGELOG.md` до commit, міграції та ручної перевірки на deploy.

---

## Архів / SYNC

- **2026-08-13** — shipped (розсилка `73911a6`, оферта `4401fbc`, нагадування 24h `de69dae`) перенесено в `CHANGELOG.md` Unreleased і `Зміни_логіки_та_функціоналу.md`; чернетку очищено.
- **2026-08-08** — перенесено shipped/ready з червня–серпня 2026 у `CHANGELOG.md` та `Зміни_логіки_та_функціоналу.md` (стисло, без дублювання сирих ~3500 рядків). Старші ready (березень–квітень) уже були в CHANGELOG після SYNC 2026-03-26 / 2026-04-02. Чернетку очищено.
- **2026-04-02** — SYNC venues / CHANGELOG / handoff.
- **2026-03-26** — SYNC ready → документи проєкту.
