# Зміни логіки та функціоналу (FIT 3.0)

Хронологія змін по проєкту. Доповнювати при змінах.

---

## 2026-06-03 — Адмін-бот: зняття власника закладу (заклад не видаляється)

- **Коміт:** `537b296`
- **`adminUnassignVenueOwner`**: delete з `venue_managers`; `venues` без змін; роль → `student`, якщо немає інших прив’язок.
- **UI:** картка закладу (`ADM_VOWN_RM` / `ADM_VOWN_OK`); картка користувача (`ADM_UVOWN` / `ADM_UVOWN_OK`); лічильник власників у статистиці.
- **Файли:** `lib/supabase.js`, `lib/adminVenues.js`, `lib/adminBot.js`

---

## 2026-06-03 — Умови користування + пошук закладів за `city_list`

- **Коміти:** `1207ab4`, `6922fb6`, `29a2ecd`, `f119b80`, `b35ff46`
- **`MENU_TERMS_OF_USE`** → `lib/termsOfUse.js` (текст за роллю).
- Пошук закладів: `VEN_LOC_OBL`, `VEN_LOC_CIT`, FSM `VENUE_LOC_OBLAST_INPUT` / `VENUE_LOC_CITY_INPUT`; `VENUES_NAME_SEARCH` — окремо.

---

## 2026-06-02 — 2026-06-03 — Роль `venue_owner` (фаза 0) та меню власника

- **Коміти:** `7c71f39` … `5b0eb65` (деталі в `WORKLOG.md`)
- Реєстрація **Власник закладу**; `venue_managers`; кабінет `lib/venueOwner.js`.
- Головне меню: **Мій заклад**, **Тренери**, **Клуби, студії**, **Зв’язок**; адмін **ADM_VOWN** — призначити власника.
- Групові на публічних картках (`coachGroupPublic.js`); підказки власника (пізніше прибрані з меню в `0fee94e`).

---

## 2026-05-20 — 2026-05-23 — Підказки тренера/учня, розклад, інвайти (sync з WORKLOG)

- **Тренер:** `MENU_HINTS`, `HINTS_*`, `coachFirstSteps.js`, `coachHints.js` (`caea7b0` та наступні); REG_AI intro (`8ca8692`, `4da4fed`).
- **Учень:** `STUDENT_HINTS_*` (`35be7f1`, `9ed2df2`).
- **Розклад:** блокування меню учня при `REQUESTED` (`f5d32a2`); coach teach/train venues (`64b8069`).
- **Адмін:** пошук, інвайти `USED_*`, збереження ролі тренера на універсальному інвайті (`01fee71`, `80513b0`, `84ee6ef`).
- **Інвайт onboarding:** роль після місця (`8dd4537`).

---

## 2026-05-19 — Адмін-бот: без дубля користувача після активації інвайту

- **Коміт:** `8ed0a12`
- **Проблема:** `replaceInviteWithChatId` створює новий рядок з реальним `chat_id`, а заготовку позначає `USED_INVITE_*` — в адмінці **👥 Користувачі** було дві кнопки на одну людину.
- **Рішення:** `adminRealUsersQuery()` — виключення `user_id ILIKE 'INVITE_%'` у списку користувачів і лічильниках статистики; активні невикористані коди лишаються в **🔑 Інвайти**.
- **Файли:** `lib/supabase.js`

---

## 2026-05-19 — Реєстрація: intro циклу/менопаузи + профіль «Цикл і менопауза»

- **Коміт:** `336510c`
- **Реєстрація (жінка, учень і тренер):**
  - Після `REG_GENDER_FEMALE` — `REG_CYCLE_INTRO`: пояснення + **«Заповнити зараз»** / **«Заповнити пізніше»** (`REG_CYCLE_FILL_NOW` / `REG_CYCLE_FILL_LATER`).
  - «Зараз» — ланцюжок статусу (регулярний / перименопауза / менопауза), довжина циклу, тривалість, дата (`CY_ST`, `CY_LEN`, `CY_BLD`, `CY_LSKP`).
  - «Пізніше» — `regCycleDeferred`, після finish — підказка відкрити **Профіль → Цикл і менопауза**.
  - Порядок кнопок ролі на `/start`: **Тренер** → **Учень**.
- **Профіль (лише жінка):**
  - **✏️ Редагувати дані → 🌸 Цикл і менопауза** — статус, довжина циклу, тривалість місячних, дата, symptom-check (`PROFILE_EDIT_CYCLE`, `PROFILE_CY_*`).
- **Файли:** `lib/registration.js`, `lib/profile.js`, `lib/constants.js`

---

## 2026-05-18 — «Мої вправи» + сповіщення про тренера в закладі

- **Коміти:** `fd40667` (вправи), `e92e07a` (notify)
- **Мої вправи:**
  - Таблиця `user_custom_exercises`; у `training_plan_exercises` — `custom_exercise_id`.
  - Меню `💪 Тренування → ⭐ Мої вправи`; ручний план — каталог + custom (`PLAN_EXERCISE:c_<uuid>`, `PLAN_GROUP:__myex__`).
  - Міграція: `supabase_migration_user_custom_exercises.sql`.
  - У верхній групі каталогу додано **Розтяжка** (між Плечі та Руки).
- **Notify тренер у закладі:**
  - Після першої `linkCoachVenue` — `lib/coachVenueNotify.js`; отримувачі — `user_venues` для `venue_id`; env `COACH_VENUE_NOTIFY_DISABLED=1`.
- **Файли:** `lib/myExercises.js`, `lib/trainingPlan.js`, `lib/menu.js`, `lib/router.js`, `lib/coachVenueNotify.js`, `lib/venues.js`, `lib/supabase.js`

---

## 2026-05-11 — Локація користувача/закладу (область/місто/район) + notify про новий заклад

- **Коміт:** `1be26af`
- **БД / міграція:**
  - Додано `supabase_migration_users_venues_location_notify.sql`.
  - `users`: нові поля `oblast`, `district`.
  - `venues`: нове поле `district`.
- **Сповіщення про новий заклад:**
  - Новий модуль `lib/venueNewNotify.js`.
  - Після `ADM_VCFM` (успішне збереження закладу) запускається фонова розсилка в основний бот.
  - Відбір отримувачів: `users.oblast == venue.oblast` + `users.city == venue.city`; якщо у закладі є `district`, тоді додатково фільтр за районом (або порожній район у користувача як wide-match по НП).
  - Роль користувача не фільтрується; службові виключення: `INVITE_%`, `is_blocked`.
- **Реєстрація та профіль:**
  - Після вибору НП додано крок `district` (опційно) з кнопкою пропуску.
  - Збереження `city/oblast/district` у `users` при реєстрації та при редагуванні міста в профілі.
- **Адмін-бот (заклади):**
  - Додано крок/поле `district` у сценарії створення та редагування закладу (`ADM_VSKD`).
  - У превʼю і в картці закладу показується район (якщо заповнений).
- **Основний бот (картка закладу):**
  - У картці закладу виводиться `district`, якщо є.

---

## 2026-03 — Розклад тренера (узгодження з кодом і документацією)

- **Налаштування шаблону:** колонка `work_hours_by_weekday`, опція «Різний час по днях тижня»; `getWorkHoursForWeekday`, `WEEKDAY_LONG_UA_MON0` (коректні назви Пн–Нд для кнопок); міграція `supabase_migration_coach_schedule_work_hours_by_weekday.sql`.
- **Мій розклад → Вільні слоти:** лише текст + `pageSlots = []`; за замовчуванням показ **на 3 дні** (від поточної дати) + кнопка **«Інші вільні слоти»** для показу решти вільних слотів у вікні 21 день; «Зайняті» / календар / слоти дня — поведінка згідно з **Бізнес-логіка** §4.4.7–4.4.8 та **CALLBACK_FSM_MODULE_MATRIX.md**.
- **Розклад → Додати слоти на день:** `showAddSlotsForDayCalendar`, `SCH_ADD_SLOTS_DAY_PICK` — §4.4.6a у бізнес-логіці.
- **Розклад → Календар → дата:**
  - Текстові підказки на екрані дати показуються **перед** списком слотів.
  - Додані підказки для дій **❌ скасувати** та **🔄 перенести** (з підтвердженням учнем); кнопку **✔️ (відмітити тренування)** прибрано з екрана календаря.
  - Підказка для “вільного” слота змінена на **`👤 Вільний`** (як на активній кнопці слота).
  - Для `BOOKED` слота кнопки в один ряд: **[час+ПІБ] [❌] [🔄]** (❌/🔄 без тексту), перша кнопка — read-only інформація по слоту.
  - Для `RESERVED` (перерва) слотів дія **«Відкрити слот»** перейменована на **«Відмінити»**; після скасування перерви повертаються активні кнопки вільного слота.
- **Розклад → Меню:** реструктуризація навігації:
  - `📆 Календар (21 день)` перенесено з підменю `Мій розклад` у верхнє меню `Розклад`.
  - `Мій розклад` перейменовано на `🕒 Слоти - праця с ними`; кнопки `Створити слоти` та `Додати слоти на день` перенесено в це підменю.
  - Створено підменю `🍔 Мої перерви \ відпустки`: сюди перенесено керування перервами та відпустками (в т.ч. `Створити перерву`, `Створити відпустку`, `Мої відпустки`); у підменю відпусток змінено порядок кнопок (спочатку `Мої відпустки`).
  - У меню `Розклад` додано підказку, як записати учня на тренування (через `Мої учні` або через `Календар`).
- **Розклад → Відмітити тренування:** оновлено підказки (прибрано “21 день”, додано автопозначення при записі через меню «Тренування», фінансову/статистичну підказку з префіксом `💰💲`, звертання “ти”).
- **Відпустки тренера:** додано перевірки, що у дні відпустки не можна записати/запросити слот (і тренеру, і учню); у календарі переносу приховано дні відпустки.
- Документація оновлена: **CALLBACK_FSM_MODULE_MATRIX.md**, **Бізнес-логіка_Gym_3_0_v1.1.md**, **README.md**.

## 2026-03 — Реєстрація (закрите бета) + оферта + зв’язок з розробником

- **Реєстрація (/start):** у режимі закритого бета кнопка **«✅ Нова реєстрація»** показує повідомлення, що доступ лише за інвайт‑кодом, та посилання на support-бот `FitHad_helpbot`.
- **Інвайт (учень):** після введення `INVITE_XXXX` перед активацією показується гейт оферти: **«📋 Читати угоду»** (текст з `OFERTA.md`) + **«✅ Приймаю умови»** / **«❌ Відмовитись»**; активація інвайту виконується лише після акцепту.
- **Меню (тренер/учень):** додано пункт **«Зв’язок з розробником»** з кнопками **«💬 Написати розробнику»** (URL) та **«📄 Читати оферту»** (показ `OFERTA.md`).
- **Оферта:** після показу тексту оферти додаються кнопки виходу **«💬 Написати розробнику»** та **«🏠 Головне меню»**.
- Документація оновлена: **Бізнес-логіка_Gym_3_0_v1.1.md**, **CALLBACK_FSM_MODULE_MATRIX.md**.

## 2026-04 — Документи тренера + підказки замірів + help-bot (завершення діалогу після інвайту)

- **Документи тренера (освіта у сфері фітнесу):**
  - Під час реєстрації тренер може завантажити документи (фото/файл), які зберігаються в БД (`coach_documents`, Telegram `file_id`).
  - Учень бачить документи в `Мій тренер → Документи тренера`.
  - Тренер керує документами у `Профіль → 📄 Мої документи`: перегляд, **додавання** з профілю, **видалення** з підтвердженням.
- **Заміри / Параметри:**
  - Додано загальну підказку, що параметри тіла можна змінювати в `Профіль → 📊 Оновити заміри`.
  - Додано розширені підказки для талії, ягодиць, біцепса, шиї, запʼястя, плечей.
  - Додано новий замір: **біцепс у напруженому стані** (`arm_flex`).
- **Help-bot (FitHad_Help):**
  - Після видачі інвайт-коду користувачу доступний ручний вихід `🏠 Гл.меню` без закриття звернення або `✅ Завершити діалог` для закриття.

## 2026-04 — Довідник закладів (venues) + пошук

- **БД (міграції в репо):** `venues`, `venue_facets`, `venue_directory_codes`, `coach_venues`, `user_venues`, `venue_schedule`; seed кодів з `fit_club_directory.md`.
- **Основний бот:** меню **«Клуби, студії»** — гео-пошук (радіус 1/3/5/10 км) і текстовий пошук (область, місто, назва + транслит); фільтр типу організації; `VENUES_*`, `REG_VENUE_*`, `PROFILE_COACH_VENUES`.
- **Реєстрація:** після вибору міста — підказка про заклади / пошук / «пізніше».
- **Адмін-бот:** розділ закладів (`lib/adminVenues.js`), додавання лише з геолокації; **групові заняття** — вибір з довідника кнопками (`ADM_VGT`, `ADM_VGP`, `ADM_VGOK`, `ADM_VGCL`, …), без ручного вводу кодів (коміт `19555d3`).
- Детальний план і журнал комітів: **`VENUES_DIRECTORY_IMPLEMENTATION_PLAN.md`**; handoff для нового чату: **`HANDOFF_VENUES_AND_SEARCH.md`**.

## 2026-04 — Довідник закладів: розширення (квітень, за `WORKLOG.md`)

- **Текстовий пошук:** якщо в профілі / state реєстрації вже є область і місто — не дублювати ввід; одразу запит назви (або `-`).
- **Адмін-бот:** область/місто закладу через **`city_list`** (`ADM_VOB`, `ADM_VCT`); превʼю закладу перед збереженням; редагування полів з превʼю (`ADM_VE:*`); **amenities** (`ADM_VAT`, …); локальна назва групового для одного закладу (`label_ua`, `ADM_VGCUST`); **картка закладу** зі списку: години (`venue_hours`), розклад групових (`venue_schedule`), телефон і соцмережі, видалення закладу; **довідник цін** у картці (`ADM_VPR*`, таблиці `venue_group_class_prices`, `venue_gym_membership_offers`, `venue_ancillary_services`); у callback цін — **`:venue_id`** для стабільності після рестарту.
- **Основний бот — UX:** хаб **«Клуби, студії»**: «Мої заклади», **🔎 Пошук нового закладу** (`VENUES_SEARCH_NEW`), deep link **`/start venue_<id>`** для тренера/учня з прив’язкою; картка закладу з блоком **«Тренери закладу»** та кнопками **`PVCH:<chatId>`**; deep link **`/start pvch_<chatId>`** → публічна картка тренера; для тренера прибрано кнопку відкриття власної картки серед `PVCH`; у профілі учня — блоки тренерів за закріпленими закладами.
- **Не входить у «ціни в залі»:** показ цін учням у основному боті (наразі лише адмін-довідник).
