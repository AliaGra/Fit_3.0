/**
 * Підказки тренера «В першу чергу» — тексти реєстрації + чекліст (зберігання в bot_state).
 */
const State = require('./state');
const { CONSTANTS } = require('./constants');

const PERSIST_KEY = 'coachFirstStepsDone';

function firstStepDirectNavHint(buttonLabels) {
  if (!buttonLabels) {
    return (
      '\n\n💡 Не обов\'язково виходити в головне меню — у пунктах з кнопками нижче можна перейти до потрібного розділу.'
    );
  }
  return (
    '\n\n💡 Не обов\'язково виходити в головне меню — ' +
    buttonLabels +
    ' можна відкрити кнопкою нижче.'
  );
}

/** 9 пунктів онбордингу (id 1–9). */
const COACH_FIRST_STEPS = Object.freeze([
  {
    id: 1,
    shortLabel: 'Розклад робочого тижня',
    body:
      '1. Спочатку налаштуй розклад свого робочого тижня: 🏠 Головне меню → 📅 Розклад → ⚙️ Налаштування розкладу.' +
      firstStepDirectNavHint('«⚙️ Налаштування розкладу»')
  },
  {
    id: 2,
    shortLabel: 'Вільні слоти',
    body:
      '2. Створи вільні слоти («вікна») для запису учнів на тренування: 🏠 Головне меню → 📅 Розклад → 🕒 Слоти → 📆 Створити слоти.' +
      firstStepDirectNavHint('«📆 Створити слоти»')
  },
  {
    id: 3,
    shortLabel: 'Додати учнів',
    body:
      '3. Додай своїх учнів на платформу: 🏠 Головне меню → 👥 Мої учні → ➕ Додати учня.\n' +
      'Після імені обов\'язково вкажи **рівень досвіду** та **дату народження** учня — тоді можна створити інвайт-код або продовжити заповнення профілю.\n' +
      'Передай код учню: **ПЕРЕШЛИ** окреме повідомлення з інвайт-кодом (не копіюй вручну).' +
      firstStepDirectNavHint('«➕ Додати учня»')
  },
  {
    id: 4,
    shortLabel: 'Запис тренувань на платформу',
    body:
      '4. Перенеси час тренувань своїх учнів на платформу одним із двох способів:\n' +
      '• 🏠 Головне меню → 📅 Розклад → 📆 Календар (21 день): обери дату й час;\n' +
      '• 🏠 Головне меню → 👥 Мої учні → Список учнів → обери учня → 🖋️ Записати.\n' +
      'Підтверджені записи переглядай у **📋 Графік роботи** (перша кнопка головного меню).' +
      firstStepDirectNavHint('«📆 Календар» або «📋 Список учнів»')
  },
  {
    id: 5,
    shortLabel: 'Пояснити учню про запис',
    body:
      '5. Поясни учню, що за потреби перенести або записатись на нове тренування йому більше не потрібно телефонувати чи писати тобі, намагаючись узгодити ваш вільний час.\n' +
      'Тепер йому достатньо увійти на платформу в телеграмі і перенести час тренування, вибравши зручний вільний слот у тебе. Ти одразу отримаєш повідомлення в телеграмі, і після твого підтвердження — в учня та у тебе зміниться час тренування.\n' +
      'Під час реєстрації по інвайту учень може обрати заклад, де тренуєш (**Де тренує тренер**), якщо ти вказав заклади в профілі.' +
      firstStepDirectNavHint(null)
  },
  {
    id: 6,
    shortLabel: 'Профіль учня',
    body:
      '6. Разом із кожним учнем заповни його профіль: так ти зможеш складати коректні плани тренувань, відстежувати динаміку тіла та прогресію навантаження:\n' +
      '🏠 Головне меню → 👥 Мої учні → Список учнів → обери учня → **📏 Обміри/Медицина/Досвід** (обміри та активність, медичний профіль, досвід).' +
      firstStepDirectNavHint('«📋 Список учнів»')
  },
  {
    id: 7,
    shortLabel: 'Плани тренувань',
    body:
      '7. Створи плани тренувань кожному учню, використовуючи бібліотеку вправ платформи та додаючи свої вправи (доступні лише тобі):\n' +
      '🏠 Головне меню → 👥 Мої учні → Список учнів → обери учня → 📋 План тренувань.' +
      firstStepDirectNavHint('«📋 Список учнів»')
  },
  {
    id: 8,
    shortLabel: 'Вартість тренувань',
    body:
      '8. Установи вартість своїх тренувань (тоді платформа буде вести статистику доходів):\n' +
      '🏠 Головне меню → 👤 Мій профіль → 💰 Вартість тренувань.\n' +
      'Якщо в учня індивідуальний тариф: 🏠 Головне меню → 👥 Мої учні → Список учнів → обери учня → 💰 Індивідуальний тариф.' +
      firstStepDirectNavHint('«💰 Вартість тренувань» або «📋 Список учнів»')
  },
  {
    id: 9,
    shortLabel: 'AI-аналітика учня',
    body:
      '9. Після заповнення профілю учня (заміри, активність, ціль) — запусти AI-аналіз тіла учня. Платформа надасть рекомендації щодо навантаження, акцентних зон і планування:\n' +
      '🏠 Головне меню → 👥 Мої учні → Список учнів → обери учня → 🤖 AI-аналітика.' +
      firstStepDirectNavHint('«📋 Список учнів»')
  }
]);

function normalizeDoneMap(raw) {
  const out = {};
  if (!raw || typeof raw !== 'object') return out;
  for (const item of COACH_FIRST_STEPS) {
    out[item.id] = !!raw[item.id] || !!raw[String(item.id)];
  }
  return out;
}

async function getDoneMap(chatId) {
  const st = await State.get(chatId);
  return normalizeDoneMap(st && st[PERSIST_KEY]);
}

async function setStepDone(chatId, stepId, done) {
  const id = parseInt(String(stepId), 10);
  if (!COACH_FIRST_STEPS.some((s) => s.id === id)) return false;
  const st = (await State.get(chatId)) || {};
  const map = normalizeDoneMap(st[PERSIST_KEY]);
  map[id] = !!done;
  await State.update(chatId, { [PERSIST_KEY]: map });
  return true;
}

async function toggleStep(chatId, stepId) {
  const id = parseInt(String(stepId), 10);
  const map = await getDoneMap(chatId);
  return setStepDone(chatId, id, !map[id]);
}

function buildRegAiIntroHintsText(role) {
  if (role === CONSTANTS.ROLES.COACH) {
    return (
      'Що тобі зробити в першу чергу:\n\n' +
      'Зайди в меню **💡 Підказки** і послідовно виконай усі кроки. ' +
      'Так ти зможеш використовувати платформу з максимальною користю для себе.\n' +
      'Після налаштування розкладу перевір **📋 Графік роботи** у головному меню — там видно підтверджені записи учнів.'
    );
  }
  return (
    '📋 **Далі в анкеті:** поточні заміри, щоденна активність і бажані параметри тіла.\n' +
    '🤖 Це потрібно для AI-аналітики та коректного плану тренувань.\n\n'
  );
}

function countDone(map) {
  let n = 0;
  for (const item of COACH_FIRST_STEPS) {
    if (map[item.id]) n++;
  }
  return n;
}

module.exports = {
  COACH_FIRST_STEPS,
  PERSIST_KEY,
  getDoneMap,
  setStepDone,
  toggleStep,
  buildRegAiIntroHintsText,
  countDone,
  normalizeDoneMap
};
