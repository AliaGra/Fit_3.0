# FIT 3.0 — правила розробки (Node.js)

**Версія:** 3.0  
**Дата:** 12.02.2026  
**Стек:** Node.js, Supabase, Telegram Bot API, Railway

---

## 1. СУТЬ ПРОЄКТУ

FIT 3.0 — Telegram-бот для тренерів та атлетів:
- Реєстрація, профіль, заміри
- Розклад тренувань (без Google Calendar)
- Мої учні, інвайт-коди
- Звіти тренера

**Архітектура:**
- **Бот:** Node.js (`index.js` + `lib/`) — **єдиний робочий runtime**. Вся логіка маршрутизації, розкладу, профілю, звітів — у `lib/`.
- **БД:** Supabase (PostgreSQL). Усі робочі дані (users, workout_schedule, pricing, logs, reminders_sent, measurements_history, exercise_library тощо) зберігаються в Supabase.
- **Хостинг:** **Railway** — бот розгорнуто на Railway. Деплой: `git push` до `main` → автоматичний білд і рестарт. Зміни в боті з'являються після успішного деплою (1–3 хв). Webhook: POST `/webhook`. Telegram webhook має вказувати на URL Railway, не на GAS.
- **GAS (Google Apps Script):** використовується **тільки** для однієї Google Таблиці — бібліотека вправ. Вправи додають/редагують у листі, потім експорт → імпорт у Supabase (`exercise_library`). Файли `*.gs` (Router, Schedule, Menu, Registration тощо) **не запускають бота** і не обробляють webhook у продакшені.
- **Google Calendar:** НЕ використовується

---

## 2. СТРУКТУРА ПРОЄКТУ

```
d:\Fit 3.0 vs2.0\
├── index.js          # Точка входу, HTTP + webhook
├── lib/
│   ├── router.js     # Маршрутизація update
│   ├── constants.js  # Enum, константи
│   ├── state.js      # FSM State (Supabase bot_state)
│   ├── helpers.js    # safeSend, sendKeyboard
│   ├── telegram.js   # API Telegram
│   ├── supabase.js   # Доступ до Supabase
│   ├── user.js       # Користувачі
│   ├── registration.js
│   ├── menu.js
│   ├── profile.js
│   ├── coach.js
│   ├── schedule.js
│   └── reports.js
├── Supabase_створення_таблиць_FIT3.sql   # Еталон схеми БД
└── Бізнес-логіка_Gym_3_0_v1.1.md         # Бізнес-правила, VETO, FSM
```

**Після змін оновлюй таблицю «Статус реалізації» в Бізнес-логіка_Gym_3_0_v1.1.md.**

---

## 3. ЕТАЛОНИ

| Що | Де |
|----|-----|
| Структура БД | `Supabase_створення_таблиць_FIT3.sql` |
| Бізнес-правила, VETO, callback_data, архітектура | `Бізнес-логіка_Gym_3_0_v1.1.md` |
| Правила для Cursor (цей файл) | `.cursorrules` |

**Файли `*.gs`** у репозиторії — це Google Apps Script: використовуються лише для бібліотеки вправ (лист + експорт у Supabase). Зміни логіки бота (меню, розклад, повідомлення) роби в **`lib/`** (Node), не в GAS.

---

## 4. VETO RULES (коротко)

❌ Магічні рядки — тільки `lib/constants.js`  
❌ callback_data з даними для FSM — дані в State  
❌ Модулі виходять за межі відповідальності  
❌ Відхилення від схеми БД  
❌ try/catch для приховування помилок  

---

## 5. GIT

```bash
cd "d:\Fit 3.0 vs2.0"
git add lib/ "Бізнес-логіка_Gym_3_0_v1.1.md"
git commit -m "опис змін"
git push
```

---

## 6. ХОСТИНГ: RAILWAY

Бот працює на **Railway**. Після `git push` до `main` Railway автоматично перезбирає й перезапускає сервіс — зміни в боті вступають у силу через 1–3 хв.

**Змінні середовища (Railway):**
- `BOT_TOKEN` — Telegram Bot API
- `SUPABASE_URL` — URL проєкту Supabase
- `SUPABASE_ANON_KEY` — anon ключ Supabase
- `REMINDER_CRON_SECRET` — секрет для endpoint `/cron/reminders?secret=XXX`
- `REMINDER_HOURS_BEFORE` — за скільки годин до тренування нагадувати (за замовч. 2)

---

## 7. РОБОТА З КОДОМ

- Українська в UI
- Двокрапка `:` в callback_data (не підкреслення)
- UserID = ChatID для звичайних користувачів
- INVITE_XXXX — маркер незавершеної реєстрації по інвайту
- **Telegram:** при відправці повідомлень з динамічним текстом (ім'я тренера, дата/час) не використовуй `parse_mode: 'Markdown'` — символи `_`, `*` у іменах/датах викликають помилку "can't parse entities". Відправляй звичайний текст або екрануй.
- Після `sendKeyboard` / `sendMessage` до учня перевіряй результат; якщо API повернув помилку — повідом тренера (наприклад, що учню треба /start).

---

## 8. ОСНОВНІ ПОНЯТТЯ ТА ТЕРМІНИ

| Термін | Пояснення |
|--------|-----------|
| **chatId** | Telegram chat ID; у таблиці `users` — `chat_id`; для слотів `workout_schedule` — `coach_id`, `student_id` (зберігати як string для узгодженості). |
| **coach_id / student_id** | У `users`: тренер має `coach_id` порожній; учень має `coach_id` = chatId тренера. У `workout_schedule`: слот прив'язаний до тренера (`coach_id`) та опційно до учня (`student_id`). |
| **Статуси слоту** | `AVAILABLE` — вільний; `REQUESTED` — учень зарезервував / тренер записав, очікує підтвердження; `BOOKED` — підтверджено; `COMPLETED` — тренування відбулось. |
| **Мій розклад (учень)** | Callback `SCH_S_MY_SCHEDULE`. Показ слотів учня: `supabase.getSlotsByStudentAndStatus(chatId, null)`; фільтр по майбутніх і статусах REQUESTED/BOOKED. У Node — `lib/schedule.js`; у продакшені не покладайся на GAS. |
| **Запис тренером учня** | Тренер обирає вільний слот → обирає учня → слот переходить у REQUESTED, `student_id` = chatId учня; учню відправляється повідомлення з кнопками Підтвердити/Відхилити (без Markdown у тексті). |
| **Бізнес-логіка** | Канонічний опис — `Бізнес-логіка_Gym_3_0_v1.1.md`. Там же: архітектура Railway + Supabase, GAS лише для бібліотеки вправ. |
| **Еталон БД** | Схема таблиць — `Supabase_створення_таблиць_FIT3.sql`. Не відхиляйся від неї без міграцій. |
